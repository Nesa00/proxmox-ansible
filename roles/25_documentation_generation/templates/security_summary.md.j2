# Security Summary Report - {{ inventory_hostname }}

**Generated:** {{ ansible_date_time.iso8601 }}  
**Hostname:** {{ inventory_hostname }}  
**IP Address:** {{ ansible_default_ipv4.address | default('Unknown') }}

---

## Security Overview

This report provides a comprehensive security audit summary for the Proxmox host system.

---

## SSH Security Status

{% if audit_ssh_config %}
### SSH Configuration
- **SSH Port:** {{ new_ssh_port | default(22) }}
- **Root Login:** {{ 'Disabled' if disable_root_login else 'Enabled' }}
- **Password Authentication:** {{ 'Disabled' if disable_password_auth else 'Enabled' }}
- **Key Authentication:** {{ 'Enabled' if not disable_password_auth else 'Disabled' }}

### SSH Audit Results
```
{{ ssh_audit.stdout | default('SSH audit information not available') }}
```

### SSH Key Permissions
```
{{ ssh_key_audit.stdout | default('SSH key audit information not available') }}
```
{% else %}
**SSH Audit:** Not performed
{% endif %}

---

## Firewall Security Status

{% if audit_firewall_rules %}
### Firewall Rules
```
{{ firewall_audit.stdout | default('Firewall audit information not available') }}
```

### Proxmox Firewall
```
{{ proxmox_firewall_audit.stdout | default('Proxmox firewall information not available') }}
```
{% else %}
**Firewall Audit:** Not performed
{% endif %}

---

## User Security Status

{% if audit_user_permissions %}
### User Accounts
```
{{ user_audit.stdout | default('User audit information not available') }}
```

### Proxmox Users
```
{{ proxmox_user_audit.stdout | default('Proxmox user information not available') }}
```
{% else %}
**User Audit:** Not performed
{% endif %}

---

## Service Security Status

{% if audit_service_status %}
### Critical Services
```
{{ service_audit.stdout | default('Service audit information not available') }}
```
{% else %}
**Service Audit:** Not performed
{% endif %}

---

## Security Packages Status

### Installed Security Packages
```
{{ security_packages_audit.stdout | default('Security packages information not available') }}
```

---

## Security Configuration Summary

### Current Security Settings
- **Fail2Ban:** {{ 'Installed' if fail2ban_enable else 'Not Installed' }}
- **AppArmor:** {{ 'Disabled' if apparmor_disable else 'Enabled' }}
- **SSH Hardening:** {{ 'Enabled' if (disable_root_login or disable_password_auth) else 'Basic' }}

---

## Security Recommendations

### High Priority
1. **SSH Hardening**
   - Change default SSH port
   - Disable root login
   - Use SSH keys instead of passwords
   - Implement fail2ban

2. **Firewall Configuration**
   - Review and update firewall rules
   - Monitor firewall logs
   - Implement network segmentation

### Medium Priority
1. **User Management**
   - Regular user account audits
   - Implement least privilege principle
   - Monitor user access

2. **Service Security**
   - Disable unnecessary services
   - Monitor service logs
   - Regular security updates

### Low Priority
1. **Additional Security Tools**
   - Consider installing ClamAV for malware scanning
   - Implement Rkhunter for rootkit detection
   - Regular security audits

---

## Risk Assessment

### Current Risk Level: {{ 'LOW' if (fail2ban_enable and not apparmor_disable and disable_root_login and disable_password_auth) else 'MEDIUM' if (fail2ban_enable or not apparmor_disable or disable_root_login or disable_password_auth) else 'HIGH' }}

### Risk Factors
- **SSH Security:** {{ 'Secure' if (disable_root_login and disable_password_auth) else 'Needs Improvement' }}
- **Firewall:** {{ 'Configured' if audit_firewall_rules else 'Not Audited' }}
- **User Management:** {{ 'Audited' if audit_user_permissions else 'Not Audited' }}
- **Service Security:** {{ 'Audited' if audit_service_status else 'Not Audited' }}

---

## Next Steps

1. **Immediate Actions**
   - Review SSH configuration
   - Update firewall rules if needed
   - Audit user accounts

2. **Short Term (1-2 weeks)**
   - Implement additional security measures
   - Set up monitoring and alerting
   - Create security policies

3. **Long Term (1-3 months)**
   - Regular security audits
   - Security training for administrators
   - Incident response planning

---

*This security summary was automatically generated by the Proxmox Ansible playbook.*
